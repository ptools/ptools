image: "python:3.7"

before_script:
  - python --version
  - pip install -r requirements/base.txt -r requirements/development.txt
  - pip install -e .


stages:
  - Test

unit_test:
  stage: Test
  script:
    - make tests
