image: "python:3.11"

before_script:
  - python --version
  - pip install -r requirements/development.txt
  - pip install -e .


stages:
  - Check
  - Test

pylint:
  stage: Check
  allow_failure: true
  script:
    - make lint

unit_test:
  stage: Test
  script:
    - make test
